---
task_id: T-913
type: feat
status: completed
created: 2026-04-19T00:00:00Z
owner: agent
---

# T-913 — [FE] 상태 관리 라이브러리 도입 (Zustand)

## 차단 사유

이 태스크의 구현 대상은 **FE 리포** (`tourwave-web`)이다.
현재 리포(`tourwave`)는 Spring Boot 백엔드 전용이며 `package.json`이 존재하지 않는다.
`tourwave` 워크트리에서 T-913 작업을 수행하면 안 된다 (GP-001: 워크트리 밖 수정 금지와 별개로, 리포 자체가 FE 코드가 없음).

## 실행 위치

```
FE 리포: /Users/jaehyun/Documents/workspace/tourwave-web
태스크 카드 (BE 리포 참조): docs/tasks/cross/T-913-fe-state-management.md
```

## FE 리포에서 실행할 명령

```bash
cd /Users/jaehyun/Documents/workspace/tourwave-web
./scripts/task-start.sh T-913          # 워크트리 생성: .worktrees/T-913/
# .worktrees/T-913/ 안에서 구현
./scripts/verify-task.sh T-913         # 검증
./scripts/task-finish.sh T-913         # push + PR
```

## 구현 대상 (FE 리포 `.worktrees/T-913/` 내)

### 파일 경로
- `src/lib/state/bookingStore.ts`
- `src/lib/state/searchStore.ts`
- `src/lib/state/uiStore.ts`
- `src/lib/state/store.ts`

### 의존성
- `package.json` — `"zustand": "^5.x.x"` 추가
- 설치 명령: `npm install zustand`

## 구현 단계

- [ ] 1. FE 리포에서 `./scripts/task-start.sh T-913` 실행
- [ ] 2. `.worktrees/T-913/`에서 `npm install zustand`
- [ ] 3. `src/lib/state/bookingStore.ts` 구현 (예약 프로세스 상태)
- [ ] 4. `src/lib/state/searchStore.ts` 구현 (검색 필터, 정렬, 페이지네이션)
- [ ] 5. `src/lib/state/uiStore.ts` 구현 (모달, 알림, 로딩)
- [ ] 6. `src/lib/state/store.ts` 구현 (통합 export)
- [ ] 7. `npm run typecheck` 통과 확인
- [ ] 8. `./scripts/verify-task.sh T-913` 통과
- [ ] 9. `git push origin feature/T-913`

## Acceptance Criteria (카드 기준)

- [ ] `npm install zustand` 통과
- [ ] `src/lib/state/` 4개 파일 생성 (booking, search, ui, store)
- [ ] 각 store에 최소 3개 action + 5개 state 속성
- [ ] TypeScript strict 모드 통과
- [ ] 'use client' 경계 주석 명시

## 영향 범위

- Blocks: T-014, T-019, T-020~T-027 (예약, 검색, 필터 상태)
- Depends on: 없음
- DO NOT TOUCH: `src/features/`, T-912 인증 모듈

## 완료 기준

- [ ] FE 리포 검증기 전부 통과 (01-build, 02-test, 03-lint, 04-security, 05-docs-freshness, 06-api-drift)
- [ ] `feature/T-913` 브랜치 push 완료
