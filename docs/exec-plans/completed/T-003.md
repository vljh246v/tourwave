---
task_id: T-003
type: refactor
phase: develop
base_branch: develop
status: in-progress
created: 2026-04-19T00:00:00Z
owner: vljh246v
---

# refactor: TimeWindowPolicy 상수 설정화 (T-003)

## 배경 및 목표

`TimeWindowPolicyService`, `OfferExpirationService`, `BookingCommandService`에 하드코딩된
시간 상수(6h 초대 차단, 48h 초대 만료, 48h 환불 기한, 86400s 오퍼 창)를
`application.yml`로 외부화하여 환경별 오버라이드를 가능하게 한다.

## 수정 파일

### 메인 소스 (6개)
1. `src/main/resources/application.yml` — `tourwave.timewindow` 섹션 추가
2. `src/main/resources/application-mysql-test.yml` — 테스트 소값 오버라이드 없음 (기존 상수값 그대로 유지)
3. `application/common/TimeWindowPolicyService.kt` — companion object 제거, 생성자 파라미터 3개
4. `application/booking/OfferExpirationService.kt` — companion object 제거, offerWindowSeconds 파라미터
5. `application/booking/BookingCommandService.kt` — companion object 제거, offerWindowSeconds 파라미터
6. `bootstrap/UseCaseConfig.kt` — @Value 주입 추가

### 테스트 (7개)
- `TimeWindowPolicyServiceTest.kt` — TimeWindowPolicyService(360, 48, 48)
- `CatalogQueryServiceTest.kt` — TimeWindowPolicyService(360, 48, 48)
- `OfferExpirationServiceTest.kt` — TimeWindowPolicyService(360, 48, 48), OfferExpirationService에 86400L 추가
- `BookingQueryServiceTest.kt` — TimeWindowPolicyService(360, 48, 48)
- `ParticipantQueryServiceTest.kt` — TimeWindowPolicyService(360, 48, 48)
- `InvitedParticipantExpirationServiceTest.kt` — TimeWindowPolicyService(360, 48, 48)
- `ParticipantCommandServiceTest.kt` — TimeWindowPolicyService(360, 48, 48)

## 구현 단계

- [x] 1. exec-plan 작성 + 워크트리 생성
- [x] 2. `application.yml` — timewindow 섹션 추가
- [x] 3. `TimeWindowPolicyService.kt` — 파라미터화
- [x] 4. `OfferExpirationService.kt` — 파라미터화
- [x] 5. `BookingCommandService.kt` — 파라미터화
- [x] 6. `UseCaseConfig.kt` — @Value 주입
- [x] 7. 테스트 7개 — 기존 상수값으로 인자 전달
- [x] 8. `verify-task.sh T-003` 검증 통과

## 완료 기준

- [x] companion object 상수 3개 제거 (TimeWindowPolicyService, OfferExpirationService, BookingCommandService)
- [x] YAML 키 4개 추가 (invitation-window-minutes, invitation-expiry-hours, refund-full-window-hours, offer-window-seconds)
- [x] 테스트 7개 생성자 인자 추가 — 기존 동작값 보존
- [x] `./gradlew test` 전체 통과
