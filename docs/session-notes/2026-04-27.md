---
date: 2026-04-27
session_type: dev-orchestration (T-923, T-907) + meta-fixes (T-006 후속, task-start.sh)
status: done (T-923/T-907 develop merged, task-start.sh 결함 수정)
---

# 2026-04-27 세션 요약

## 수행한 작업

### 1. 블록커 분석 (`/blockers`)
- 미완료 카드 2개(T-903, T-923) 모두 `depends_on: []`, `blocks: []`
- 추천 → **T-923** (size M, infra) 먼저

### 2. T-923 — publish-tasks.sh YAML frontmatter 포맷 재작성 (BE-only)
- 카드: `docs/tasks/cross/T-923-publish-tasks-yaml-migrate.md`
- 변경: `scripts/publish-tasks.sh` 1파일 (+68/-35)
  - `frontmatter_get_field` 유틸 신규 (awk 기반 YAML 파싱)
  - 구 리스트 포맷 `extract_meta()` 제거
  - `github_issue:` 숫자=skip / null=publish 대상 판정
  - 발행 후 `github_issue: null` → `github_issue: <num>` sed 교체
- verify-task.sh: PASS 7/7, security CLEAN
- merge: `1d4f528` → develop, dry-run 18카드(7 skip + 11 publish 대상) 인식 확인

### 3. T-006 사후 점검 → 결함 발견 (F-2026-04-27-1)
사용자 요청으로 직전 완료 T-006 작업 검증.
- **결함 A (Phase 7.5 sync 협소)**: orchestrator Phase 7.5는 openapi/schema/policies/domain-rules만 propagate, audit/gap-matrix/testing.md/exec-plan 본문은 미커버
- **결함 B (exec-plan 본문 누락)**: `docs/exec-plans/completed/T-006.md`가 빈 템플릿(`[작업 목표를 작성하세요]`) 그대로 archived. 회고/결정 기록 0
- **결함 C (WRITE↔실제 src 정합성 부재)**: T-006 카드 WRITE는 `adapter/in/web/user/MeControllerIntegrationTest.kt`였으나 실제 MeController는 `adapter/in/web/auth/`. tdd-backend가 카드 따라 user/에 만든 뒤 auth/에도 동일 7케이스 생성, user/ 미정리. verify-task.sh는 둘 다 PASS이라 검출 불가
- 즉시 정리: `3c4a38b` user/ 중복 삭제 (-157)

### 4. failure-patterns 갱신 + T-907 카드 등재
- `logs/trends/failure-patterns.md` "2026-04-27 / 작업 완료 후 문서·정합성 누락 (T-006)" 항목 추가
- `docs/tasks/cross/T-907-orchestrator-doc-sync-and-cleanup-guards.md` 신설 (Phase 0/7.6/8.5 + tdd/verification 의무 보강)

### 5. T-907 — orchestrator 문서·정합성 가드 (BE-only / infra·tooling)
- escalation 판정: `~/.claude/agents/*.md`는 모든 미래 `/dev` 행동 제어하는 거버넌스 표면 → 사용자 승인 후 진행 (escalation-policy 항목 7 유사)
- 프로토콜: 워크트리 안 `agents-proposed/`에 변경안 작성·commit (GP-007 위반 회피) → 사용자 승인 후 `~/.claude/agents/`에 cp
- 산출 (5 stage commits, 7 files):
  - `9ba4562` agents-proposed/ 4개 파일 (orchestrator/tdd-backend/tdd-frontend/verification)
  - `fab783d` exec-plan 본문 채움 (dogfood)
  - `f208e2b` agent-failures F-2026-04-27-1 등재
  - `cfb5f98` gap-matrix T-907 행 추가
  - `f2b651c` exec-plan 체크박스 완료
- 적용된 글로벌 변경:
  - **orchestrator.md**: Phase 0 sub-step(WRITE 경로 정합성), Phase 7.6(문서 동기화 5단계), Phase 8.5(잔재·중복 탐지)
  - **tdd-backend.md / tdd-frontend.md**: Step 2.5(WRITE↔실제 패키지 대조 + BLOCKER 의무), Step 9(exec-plan 본문 채움 의무), 양쪽 동시 생성 절대 금지
  - **verification.md**: Step 7.5(빈 템플릿/audit/gap-matrix/메인 repo 누출 검사), PASS-only 보고 금지, 실패 가이드 표 2행 추가
- 범위 축소(advisor 권고): `scripts/validators/08-doc-consistency.sh` deferred (false-positive 위험)
- 사용자가 직접 `cp` 실행 후 `task-finish.sh` 진행
- merge: `2bbdb0d` → develop (단, 1차 시도 실패)

### 6. F-2026-04-27 부수 결함: task-start.sh 빈 스켈레톤 위치 오류
- T-907 1차 task-finish.sh 실패: "untracked working tree files would be overwritten"
- 원인: `scripts/task-start.sh` line 85 `EXEC_PLAN_DIR="${PROJECT_ROOT}/..."` — 메인 repo에 빈 스켈레톤 작성. 워크트리는 자체 워킹트리에 같은 경로 추적 파일 commit. develop 머지 시 "메인 untracked" vs "워크트리 tracked" 충돌
- 수동 복구: 메인 repo 빈 파일 `rm` 후 finish 재시도 → 정상 머지
- 즉시 수정: `540bfcf` `EXEC_PLAN_DIR="${WORKTREE_PATH}/..."`로 변경. 다음 `/dev` 호출부터 재발 안 함

### 7. /caveman 모드 활성 + feedback 메모리 등재
- 세션 전반 caveman full 모드 활성 (system 훅)
- `feedback_auto_task_finish.md` 신설: orchestrator DONE 직후 task-finish.sh 자동 실행 (verify PASS 한정)
  - 사용자 발화: "왜 자꾸 task-finish.sh 나한테 물어봐, 그냥 자동으로 실행하면 안돼?"

---

## 커밋 흐름 (이번 세션)
```
540bfcf fix: task-start.sh exec-plan 생성 위치 워크트리로 정정
19e6f31 chore: T-907 검증 로그 + exec-plan 정리 + status → done
2bbdb0d merge: T-907
f2b651c stage: T-907 exec-plan 완료기준 체크박스 갱신
cfb5f98 stage: T-907 gap-matrix T-907 항목 추가
f208e2b stage: T-907 agent-failures F-2026-04-27-1 등재
fab783d stage: T-907 exec-plan 본문 채움 (dogfood)
9ba4562 stage: T-907 agents-proposed/ 4개 파일 작성
5f89100 chore: T-907 작업 시작 (status → in-progress)
98e34b1 docs: T-006 사후 분석 — failure-patterns 갱신 + T-907 신규 카드
3c4a38b chore: T-006 후속 정리 — 중복 MeControllerIntegrationTest 제거
402f4c3 chore: T-923 검증 로그 + exec-plan 정리 + status → done
1d4f528 merge: T-923
b23aeac stage: Refactor — publish-tasks.sh card_id 제거
cf8d873 stage: Green — publish-tasks.sh YAML frontmatter 파싱 재작성
260fbed chore: T-923 작업 시작 (status → in-progress)
```
develop이 origin 대비 **+22 commits ahead** (이번 세션 16 + 직전 6).

---

## 남은 작업

### A. push (사용자 승인 필요)
```bash
cd /Users/jaehyun/Documents/workspace/tourwave
git push origin develop
```

### B. 다음 카드 후보
- **T-903** — [BE] Idempotency-Key 일관성 검토 (announcement/operations/organization/instructor) — size L. 현재 유일하게 남은 backlog 카드.
- T-907 새 가드 dogfooding 첫 실측 대상이 될 수 있음 (Phase 0 WRITE 경로 사전 체크가 5개 컨트롤러 경로에서 작동하는지 관찰).

### C. 잠재 후속 카드 (등록 보류)
- task-start.sh 결함은 즉시 수정으로 해결 — 별도 카드 불필요.
- `scripts/validators/08-doc-consistency.sh` (T-907에서 deferred) — 다음 카드 후보. 빈 템플릿 검출 + 동일 클래스명 테스트 중복 검출.

---

## 메모/주의사항

- **T-907 적용 후 첫 실측 필요**: 다음 `/dev` 호출에서 Phase 0 sub-step / Phase 7.6 / Phase 8.5가 실제로 실행되는지, dialog.md에 기록되는지 확인. 누락 시 orchestrator.md 문법/지시 보강.
- **task-start.sh 결함 수정(`540bfcf`) 검증 미실시**: 다음 `/dev` 호출이 자연 검증. 별도 단위 테스트 없음.
- **agents-proposed/ 디렉터리**: T-907 워크트리 commit으로 develop에도 보존됨 (영구 PR 기록 목적). 향후 동일 패턴(거버넌스 파일 staging) 채택 시 재사용.
- **글로벌 agents 적용 완료** (`~/.claude/agents/orchestrator.md` 등 4파일 mtime 2026-04-27 01:11). 이 세션 종료 후 새 세션에서도 즉시 적용됨.
- **현재 backlog 1건만**: T-903. 끝나면 새 카드 발굴 또는 마일스톤 진척 평가 필요.
