---
task_id: T-901
type: test
status: done
created: 2026-04-18T17:10:59Z
owner: agent
---

# T-901 — AuditEventTest 생성 (감사 이벤트 커버리지)

## 배경 및 목표

`CLAUDE.md`에 알려진 커버리지 미비 항목으로 명시된 `AuditEventTest` 부재를 해소한다.
감사 이벤트 클래스에 대한 순수 단위 테스트를 작성하여 `docs/domain-rules.md` 감사 규칙 커버리지를 확보한다.

## 구조 드리프트 결정 기록

카드(`T-901-be-audit-event-test.md`)는 `src/main/kotlin/com/demo/tourwave/domain/common/AuditEvent.kt` 파일을
대상으로 테스트를 작성하도록 지시하였으나, 해당 파일은 존재하지 않는다.

실제로 존재하는 클래스:
- `src/main/kotlin/com/demo/tourwave/application/common/port/AuditEventPort.kt`
  - `AuditEventCommand` data class (actor, action, resourceType, resourceId, occurredAtUtc, ...)
  - `AuditEventPort` interface
  - `AuditActorType` enum (USER/OPERATOR/SYSTEM/JOB)
- `src/main/kotlin/com/demo/tourwave/application/common/port/AuditEventSubscriber.kt`

결정: 카드에서 가정한 phantom 도메인 엔티티를 새로 생성하지 않는다 (Size S / type test 태스크에 entity 추가는 범위 초과).
대신 실제 존재하는 `AuditEventCommand` 및 `AuditActorType`에 대한 순수 단위 테스트를 작성한다.
테스트 파일 위치도 클래스 레이어에 맞게 조정:
- 원래 카드: `src/test/kotlin/com/demo/tourwave/domain/common/AuditEventTest.kt`
- 실제 적용: `src/test/kotlin/com/demo/tourwave/application/common/port/AuditEventCommandTest.kt`

Spring/DB/Testcontainers 미사용 조건은 동일하게 준수 (application layer test는 순수 단위 테스트 허용).

## 구현 단계

- [x] 1. task-start.sh 실행 (워크트리 생성)
- [x] 2. 대상 소스 파악 (AuditEventCommand 구조 확인)
- [x] 3. exec-plan 작성 (드리프트 결정 기록)
- [x] 4. AuditEventCommandTest.kt 작성
- [x] 5. ./gradlew test --tests "*.AuditEventCommandTest" 실행
- [x] 6. verify-task.sh T-901 실행 (PASS 5/5)
- [x] 7. git push origin feature/T-901

## 영향 범위

- WRITE: `src/test/kotlin/com/demo/tourwave/application/common/port/AuditEventCommandTest.kt` (신규)
- DO NOT TOUCH: 기존 테스트 클래스, 소스 코드

## 완료 기준

- [x] `./gradlew test --tests "*.AuditEventCommandTest"` 통과
- [x] 최소 8개 테스트 케이스 (총 11개 작성)
- [x] Spring import 없음 (pure unit test)
- [x] `AuditActorType` 파싱 로직 커버
- [x] `AuditEventCommand` 기본값 및 필드 동작 커버

## 참고

- `docs/golden-principles.md` — GP-002: domain 레이어에 Spring/JPA/Web import 금지
- `docs/domain-rules.md` — 감사 이벤트 섹션 B
- `docs/testing.md` — 레이어별 테스트 규율
